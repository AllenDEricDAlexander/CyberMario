# CyberMario

CyberMario

### Dynamic MCP Client Management

CyberMario can manage remote MCP servers from the admin console. Stage one supports Streamable HTTP and SSE transports.
New MCP servers and tools are disabled by default; tools that require confirmation are not exposed to ReactAgent until a
later confirmation workflow is added.

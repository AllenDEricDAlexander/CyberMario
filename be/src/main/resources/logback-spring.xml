<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="30 seconds">

    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="CyberMario"/>
    <springProperty scope="context" name="APP_ENV" source="spring.profiles.active" defaultValue="default"/>

    <contextName>${APP_NAME}</contextName>

    <property name="LOG_PATH" value="${CYBER_MARIO_LOG_PATH:-./logs}/${APP_NAME}"/>
    <property name="MAX_HISTORY" value="${CYBER_MARIO_LOG_MAX_HISTORY:-30}"/>
    <property name="MAX_FILE_SIZE" value="${CYBER_MARIO_LOG_MAX_FILE_SIZE:-100MB}"/>
    <property name="TOTAL_SIZE_CAP" value="${CYBER_MARIO_LOG_TOTAL_SIZE_CAP:-5GB}"/>

    <property name="TRACE_LOG_PATTERN"
              value="traceId=%X{traceId:-} requestId=%X{requestId:-}"/>
    <property name="BIZ_LOG_PATTERN"
              value="module=%X{module:-} scene=%X{scene:-} action=%X{action:-} step=%X{step:-} bizId=%X{bizId:-} result=%X{result:-} reason=%X{reason:-} errorMsg=%X{errorMsg:-} costMs=%X{costMs:-}"/>
    <property name="FILE_LOG_PATTERN"
              value="time=%d{yyyy-MM-dd HH:mm:ss.SSS} level=%-5level app=${APP_NAME} env=${APP_ENV} pid=${PID:-} thread=%thread logger=%logger{50} ${TRACE_LOG_PATTERN} ${BIZ_LOG_PATTERN} msg=&quot;%msg&quot;%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>

    <conversionRule conversionWord="clr" class="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="wex"
                    class="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>
    <conversionRule conversionWord="wEx"
                    class="org.springframework.boot.logging.logback.ExtendedWhitespaceThrowableProxyConverter"/>

    <property name="CONSOLE_LOG_PATTERN"
              value="%clr(%d{yyyy-MM-dd HH:mm:ss.SSS}){faint} %clr(${LOG_LEVEL_PATTERN:-%5p}) %clr(${PID:- }){magenta} %clr(---){faint} %clr([%thread]){faint} %clr(%logger{40}){cyan} %clr(:){faint} ${TRACE_LOG_PATTERN} ${BIZ_LOG_PATTERN} msg=&quot;%msg&quot;%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <appender name="APP_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_PATH}/application.log</file>
        <encoder>
            <pattern>${FILE_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_PATH}/application.%d{yyyy-MM-dd}.%i.log.gz</fileNamePattern>
            <maxFileSize>${MAX_FILE_SIZE}</maxFileSize>
            <maxHistory>${MAX_HISTORY}</maxHistory>
            <totalSizeCap>${TOTAL_SIZE_CAP}</totalSizeCap>
        </rollingPolicy>
    </appender>

    <appender name="ASYNC_APP_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <queueSize>1024</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <includeCallerData>false</includeCallerData>
        <appender-ref ref="APP_FILE"/>
    </appender>

    <logger name="org.springframework.web" level="INFO"/>
    <logger name="org.springframework.security" level="INFO"/>
    <logger name="org.hibernate.SQL" level="WARN"/>

    <springProfile name="dev,local">
        <logger name="top.egon.mario" level="DEBUG"/>
    </springProfile>

    <root level="${LOG_LEVEL_ROOT:-INFO}">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_APP_FILE"/>
    </root>
</configuration>

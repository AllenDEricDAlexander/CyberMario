package top.egon.mario.room.policy;

public enum RoomMutation {
    CREATE_ROOM,
    ENTER_ROOM,
    INVITE,
    ACCEPT_INVITATION,
    DECLINE_INVITATION,
    KICK,
    BAN,
    HEARTBEAT,
    REFRESH_RESERVATIONS
}
